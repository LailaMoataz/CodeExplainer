I have a finished local project called "Code Explainer for Students" — a
Streamlit app in Python that I want to publish as a public GitHub repository.
I want you to act as my step-by-step guide and walk me through this ONE STEP
AT A TIME. Don't give me the whole process in one message — give me one
instruction, wait for me to tell you what happened (it worked / I got an
error / I'm confused), and only then give me the next step.

Here's my situation:
- I already have the project folder on my computer with these files: app.py,
  requirements.txt, .env.example, .gitignore, LICENSE, CONTRIBUTING.md,
  README.md
- I have a GitHub account, username LailaMoataz
- I want the repo to be called "code-explainer"
- I am comfortable using the terminal but I'm not a Git expert, so explain
  any git command you give me in one plain sentence before I run it
- I have NOT run "git init" yet — assume I'm starting from zero in this
  folder

Walk me through, one step at a time, in this order:
1. Initializing git in the project folder
2. Making sure .env is actually ignored and my API key won't be committed
   (double-check this before the first commit, it matters)
3. Making the first commit
4. Creating the repository on GitHub (public, no README/license/gitignore
   generated on GitHub's side since I already have those locally)
5. Connecting my local folder to the GitHub repo and pushing for the first
   time
6. Verifying everything shows up correctly on GitHub, including that .env
   is NOT visible in the repo
7. Adding a short repo description and topics/tags on GitHub (e.g. python,
   streamlit, gemini-api, education, genai) so it's easier to discover
8. Pinning this repo on my GitHub profile
9. (Optional, ask me first) Deploying it live on Streamlit Community Cloud
   so people can try it without installing anything, and adding that link
   to the README

At each step:
- Give me the exact command or exact click-path to follow
- Tell me what I should see if it worked
- Tell me the most common way this step goes wrong and how to fix it
- Then STOP and wait for me to respond before moving to the next step

Start with step 1 now.
